Put PDF.js here for offline/local loading:

Required files (exact names):
- pdf.min.js
- pdf.worker.min.js

Download from:
https://github.com/mozilla/pdfjs-dist/tree/master/build
(Open each file → 'Download raw file')

After placing those two files in this 'libs' folder,
index.html will automatically use them (no CDN required).
